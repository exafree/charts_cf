name: charts_flutter_community
version: 0.10.0
description: Material Design charting library for flutter.
homepage: https://github.com/exafree/charts_community

environment:
  sdk: '>=1.23.0 <3.0.0'

dependencies:
  # Pointing this to a local path allows for pointing to the latest code
  # in Github for open source development.
  #
  # The pub version of charts_flutter_community will point to the pub version of charts_common_community.
  # The latest pub version is commented out and shown below as an example.
  #charts_common_community: ^0.10.0
  charts_common_community:
    path: ../charts_common_community/
  collection: ^1.14.5
  flutter:
    sdk: flutter
  intl: '>=0.15.2 < 0.17.0'
  logging: any
  meta: ^1.1.1


dev_dependencies:
  mockito:
  flutter_test:
    sdk: flutter
  test: ^1.3.0
